/**
 * Re-export from the library binding.
 * Examples import from here; the actual implementation lives in src/bindings/react/.
 */
export { EcsContext, createEcsHooks } from '../../src/bindings/react';
